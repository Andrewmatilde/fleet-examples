apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    app: guestbook
    tier: frontend
spec:
  # FORWARD chain test - start 14:36
  type: NodePort
  ports:
  - port: 80
  selector:
    app: guestbook
    tier: frontend
